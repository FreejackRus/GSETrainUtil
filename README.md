# 🚂 GSE Train Utility

Система управления заявками на обслуживание железнодорожного оборудования GSE (Ground Support Equipment).

## 📋 Описание

GSE Train Utility - это веб-приложение для управления заявками на техническое обслуживание и ремонт оборудования железнодорожного транспорта. Система позволяет создавать, отслеживать и управлять заявками, а также ведет учет выполненных работ.

### ✨ Основные возможности

- 🔐 **Аутентификация и авторизация** - система ролей (администратор, инженер)
- 📝 **Управление заявками** - создание, редактирование, отслеживание статуса
- 💾 **Черновики заявок** - сохранение незавершенных заявок
- 📊 **Отчетность** - журнал выполненных работ и статистика
- 🚂 **Управление поездами и вагонами** - учет подвижного состава
- 🔧 **Управление оборудованием** - каталог и статус оборудования
- 📱 **Адаптивный интерфейс** - работа на различных устройствах
- 📸 **Загрузка фотографий** - документирование состояния оборудования с поддержкой компактного режима
- 📁 **Управление файлами** - загрузка и получение файлов

## 🏗️ Архитектура проекта

Проект построен по принципам **Feature-Sliced Design** и состоит из двух основных частей:

### Frontend (React + TypeScript + Vite)
- **React 19** с TypeScript для типобезопасности
- **Material-UI** для современного пользовательского интерфейса
- **Vite** для быстрой разработки и сборки
- **Axios** для HTTP-запросов к API
- **React Router** для навигации

### Backend (Node.js + Express + Prisma)
- **Express.js** для создания REST API
- **Prisma ORM** для работы с базой данных PostgreSQL
- **JWT** для аутентификации пользователей
- **bcrypt** для безопасного хеширования паролей
- **CORS** для кросс-доменных запросов

### База данных (PostgreSQL)
Структура включает таблицы для:
- Пользователей и авторизации
- Журнала технических работ
- Справочников (типы работ, поезда, вагоны, оборудование)
- Устройств и их состояний

## 🚀 Быстрый старт

### Предварительные требования
- Node.js (версия 18 или выше)
- PostgreSQL (версия 12 или выше)
- Yarn или npm

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd GSETrainUtil
```

### 2. Настройка Backend

#### Установка зависимостей
```bash
cd backend
yarn install
```

#### Настройка базы данных
Создайте файл `.env` в папке `backend`:
```env
DATABASE_URL="postgresql://username:password@localhost:5432/gse_train_util?schema=public"
JWT_SECRET="your-secret-key-here"
PORT=3001
```

#### Инициализация базы данных
```bash
# Применение миграций
npx prisma migrate dev --name init

# Генерация Prisma Client
npx prisma generate
```

#### Запуск backend сервера
```bash
yarn dev
```
Сервер будет доступен по адресу: `http://localhost:3001`

### 3. Настройка Frontend

#### Установка зависимостей
```bash
cd frontend
yarn install
```

#### Запуск frontend приложения
```bash
yarn dev
```
Приложение будет доступно по адресу: `http://localhost:5173`

## 📁 Структура проекта

```
GSETrainUtil/
├── backend/                    # Backend приложение
│   ├── src/
│   │   ├── controllers/        # Контроллеры API
│   │   ├── middleware/         # Middleware функции
│   │   ├── router/            # Маршруты API
│   │   ├── types/             # TypeScript типы
│   │   ├── utils/             # Утилиты
│   │   └── server.ts          # Главный файл сервера
│   ├── prisma/
│   │   ├── migrations/        # Миграции базы данных
│   │   └── schema.prisma      # Схема базы данных
│   └── package.json
├── frontend/                   # Frontend приложение
│   ├── src/
│   │   ├── app/               # Основная логика приложения
│   │   ├── pages/             # Страницы приложения
│   │   ├── widgets/           # Виджеты (крупные компоненты)
│   │   ├── features/          # Функциональные модули
│   │   ├── entities/          # Бизнес-сущности
│   │   ├── shared/            # Общие компоненты и утилиты
│   │   │   └── ui/            # UI компоненты
│   │   │       ├── Button/    # Кнопки
│   │   │       ├── Header/    # Заголовки
│   │   │       ├── PhotoUpload/ # Загрузка фото
│   │   │       └── Steps/     # Компоненты шагов форм
│   │   └── main.tsx           # Точка входа
│   └── package.json
└── README.md
```

## 🔧 Доступные команды

### Backend
```bash
yarn dev          # Запуск в режиме разработки
yarn build        # Сборка проекта
yarn start        # Запуск production версии
```

### Frontend
```bash
yarn dev          # Запуск dev сервера
yarn build        # Сборка для production
yarn preview      # Предварительный просмотр сборки
yarn lint         # Проверка кода линтером
```

### База данных
```bash
npx prisma studio              # Открыть Prisma Studio
npx prisma migrate dev         # Создать и применить миграцию
npx prisma migrate reset       # Сброс базы данных
npx prisma generate           # Генерация Prisma Client
npx prisma db push            # Синхронизация схемы с БД
```

## 🛠️ Технологический стек

### Frontend
- **React 19** - UI библиотека
- **TypeScript** - типизированный JavaScript
- **Vite** - инструмент сборки
- **Material-UI** - компоненты интерфейса
- **Axios** - HTTP клиент
- **React Router** - маршрутизация

### Backend
- **Node.js** - серверная платформа
- **Express.js** - веб-фреймворк
- **TypeScript** - типизированный JavaScript
- **Prisma** - ORM для работы с БД
- **PostgreSQL** - реляционная база данных
- **JWT** - аутентификация
- **bcrypt** - хеширование паролей

## 🔐 Безопасность

- Пароли хешируются с использованием bcrypt
- JWT токены для аутентификации
- CORS настроен для безопасных кросс-доменных запросов
- Валидация данных на уровне API

## 📝 API Документация

### Основные эндпоинты:

#### Аутентификация
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/register` - Регистрация пользователя

#### Технические работы
- `GET /api/technical-works` - Получить список работ
- `POST /api/technical-works` - Создать новую заявку
- `PUT /api/technical-works/:id` - Обновить заявку
- `DELETE /api/technical-works/:id` - Удалить заявку

#### Справочники
- `GET /api/trains` - Список поездов
- `GET /api/carriages` - Список вагонов
- `GET /api/equipment` - Список оборудования

## 🤝 Участие в разработке

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add some amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Данное программное обеспечение предназначено исключительно для внутреннего использования в рамках **Группы Компаний "Перемена"** и ее дочерних организаций.

**Основные условия:**
- ✅ Использование разрешено только сотрудникам ГК Перемена
- ✅ Модификация под нужды организации разрешена
- ❌ Запрещается распространение за пределы организации
- ❌ Запрещается коммерческое использование третьими лицами

Подробные условия использования см. в файле [LICENSE](LICENSE).

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории или свяжитесь с командой разработки.

---

**Версия:** 1.0.0  
**Последнее обновление:** Январь 2025